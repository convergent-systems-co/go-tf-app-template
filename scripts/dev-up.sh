#!/usr/bin/env bash
# scripts/dev-up.sh — bring up local dev dependencies.
set -euo pipefail
echo "Replace this stub with the dev-up flow for your project."
echo "Examples: docker-compose up, terraform apply ENV=dev, etc."
